import { 
        Grid, 
        styled } 
from '@mui/material';

export const MainBody = styled(Grid)(() => ({
    justifyContent: "center",
    marginTop: '100px',
    marginBottom: '20px', 
    textAlign: 'center',
}));
