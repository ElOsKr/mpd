import { Grid, styled } from '@mui/material'

export const MainBody = styled(Grid)(() => ({
    justifyContent: "center",
    marginTop: '120px',
    marginBottom: '50px', 
    textAlign: 'center',
    height: '70vh'
}));
